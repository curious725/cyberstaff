# README
```
sudo docker-compose run web rake db:create
sudo docker-compose run web yarn install
sudo docker-compose run web rake db:enable:hstore
sudo docker-compose run web rake db:migrate
sudo docker-compose build
```


```
sudo docker-compose run web rails webpacker:install
sudo docker-compose run web rails webpacker:install:react
sudo docker-compose run web rails generate react:install
```
