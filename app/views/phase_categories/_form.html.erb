<%= form_with(model: phase_category, local: true) do |form| %>
  <% if phase_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phase_category.errors.count, "error") %> prohibited this phase_category from being saved:</h2>

      <ul>
      <% phase_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
